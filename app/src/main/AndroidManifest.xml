<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projects.irfan.letsbeginthegame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"

        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".basename"></activity>
        <activity android:name=".folksnames"></activity>
        <activity android:name=".showfolks"></activity>
        <activity android:name=".transactions"></activity>
        <activity android:name=".dues"></activity>
        <activity android:name=".summary"></activity>
        <activity android:name=".aboutDeveloper"></activity>


    </application>

</manifest>